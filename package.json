{
  "name": "t3d-whirl",
  "version": "0.5.1",
  "private": true,
  "type": "module",
  "description": "Whirl — a gravity roguelite. Fall inward, relight the dark.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.2",
    "vitest": "^2.0.5"
  }
}
